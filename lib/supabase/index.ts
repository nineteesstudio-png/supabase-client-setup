/**
 * Supabase Client Exports
 * 
 * Usage:
 * - Client-side: import { createClient } from "@/lib/supabase/client"
 * - Server-side: import { createClient } from "@/lib/supabase/server"
 * - Types: import type { Database, Stock } from "@/lib/supabase/types"
 */

export type { Database, Stock, Tables, InsertTables, UpdateTables } from "./types"
